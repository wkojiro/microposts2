<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
    <%#= render @microposts %>
    
  </ol>
  <%= paginate @feed_items %>  <%= page_entries_info( @feed_items ) %>
<% end %>