<%= form_for(current_user.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :favuser_id, current_user.id %></div>
  <%= f.submit "Favorite", class: "btn btn-primary btn-xs" %>
<% end %>
